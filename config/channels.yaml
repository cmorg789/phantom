# slack:
#   enabled: true
#   bot_token: ${SLACK_BOT_TOKEN}
#   app_token: ${SLACK_APP_TOKEN}
#   owner_user_id: ${OWNER_SLACK_USER_ID}

discord:
  enabled: true
  bot_token: ${DISCORD_BOT_TOKEN}
  guild_id: ${DISCORD_GUILD_ID}
  owner_user_id: ${DISCORD_OWNER_USER_ID}
